{% extends 'base.front.html.twig' %}

{% block title %}Commande{% endblock %}

{% block body %}
    <h1>Commande</h1>

    <table class="table">
        <tbody>
            <tr>
                <th>IdCmd</th>
                <td>{{ commande.idCmd }}</td>
            </tr>
            <tr>
                <th>Nom</th>
                <td>{{ commande.nom }}</td>
            </tr>
            <tr>
                <th>Prenom</th>
                <td>{{ commande.prenom }}</td>
            </tr>
            <tr>
                <th>NumTel</th>
                <td>{{ commande.numTel }}</td>
            </tr>
            <tr>
                <th>Codepostal</th>
                <td>{{ commande.codepostal }}</td>
            </tr>
            <tr>
                <th>Etat</th>
                <td>{{ commande.etat }}</td>
            </tr>
            <tr>
                <th>Adressmail</th>
                <td>{{ commande.adressmail }}</td>
            </tr>
            <tr>
                <th>ModePaiment</th>
                <td>{{ commande.modePaiment }}</td>
            </tr>
        </tbody>
    </table>

    <a href="{{ path('app_commandefront_index') }}">back to list</a>

    <a href="{{ path('app_commandefront_edit', {'idCmd': commande.idCmd}) }}">edit</a>

    {{ include('commandefront/_delete_form.html.twig') }}
{% endblock %}
