{% extends 'base.html.twig' %}

{% block title %}Equipement{% endblock %}
{% block body %}
    <h1></h1>
    <table class="table">
        <tbody>
            <tr>
                <th>RefEquipement</th>
                <td>{{ equipement.refEquipement }}</td>
            </tr>
            <tr>
                <th>NomEquipement</th>
                <td>{{ equipement.nomEquipement }}</td>
            </tr>
            <tr>
                <th>Image</th>
             <td><img src="{{ asset('Upload/')}}{{ equipement.image }}"alt=""style="width: 200px;height:200px"></td>
            </tr>
            <tr>
                <th>PrixEquipement</th>
                <td>{{ equipement.prixEquipement }}</td>
            </tr>
            <tr>
                <th>DescriptionEquipement</th>
                <td>{{ equipement.descriptionEquipement }}</td>
            </tr>
            <tr>
                <th>QR</th>
                <td>{% if qrCode %}
                <img src="{{ qrCode }}" style="border-radius:0!important;width: 200px;height:200px" alt="qr-code">
            {% endif %}</td>
            </tr>
        </tbody>
    </table>
    <div class="row">
        <div class="col-md-6"></div>
        <div class="col-md-3 float-right">
            
        </div>
    <a href="{{ path('app_equipement_index') }}">back to list</a>
    <a href="{{ path('app_equipement_edit', {'refEquipement': equipement.refEquipement}) }}">edit</a>

    {{ include('equipement/_delete_form.html.twig') }}
{% endblock %}
