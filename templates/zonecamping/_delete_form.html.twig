<form method="post" action="{{ path('app_zonecamping_delete', {'id': zonecamping.id}) }}" onsubmit="return confirm('Are you sure you want to delete this item?');">
    <input type="hidden" name="_token" value="{{ csrf_token('delete' ~ zonecamping.id) }}">
    <button class="btn">Delete</button>
</form>
