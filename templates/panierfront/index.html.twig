{% extends 'base.front.html.twig' %}

{% block title %}Panier index{% endblock %}

{% block body %}
<section>
<div class="container">

    <h1>Panier</h1>

    <table class="table">
        <thead>
            <tr>
                <th>Produit</th>
                <th>TotalPanier</th>
                <th>Quantité</th>
                <th>PrixEquipement</th>
                <th>actions</th>
            </tr>
        </thead>
        <tbody>
         {% for panier in paniers %}

        
        
            <tr>
                <td></td>
                <td>{{ panier.totalPanier }}</td>
                <td>{{ panier.nbrEquipement }}</td>
                <td>{{ panier.prixEquipement }}</td>
                <td>
                    <a href="{{ path('app_panierfront_show', {'idPanier': panier.idPanier}) }}">show</a>
                    <a href="{{ path('app_panierfront_edit', {'idPanier': panier.idPanier}) }}">edit</a>
                    <a href="{{ path('app_panierfront_new', {'idPanier': panier.idPanier}) }}">Add</a>
                </td>
            </tr>
        {% else %}
            <tr>
                <td colspan="5">no records found</td>
            </tr>
        {% endfor %}
       
        </tbody>
         
    </table>
    <a href="{{path('app_commandefront_new') }}">passer votre commande</a>
</div>
</section>
    
   
{% endblock %}
