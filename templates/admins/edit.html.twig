{% extends 'base.html.twig' %}

{% block title %}Edit Admin{% endblock %}

{% block body %}
<section>
<div class="container">
    <h1>Edit Admin</h1>

    {{ include('admins/_form.html.twig', {'button_label': 'Update'}) }}
    <div>
        <div class="form-group text-center">
            <ul>
        <a class="btn btn-dark btn-icon-text" href="{{ path('app_admins_index') }}">back to list</a>
            <br><br>
        {{ include('admins/_delete_form.html.twig') }}
            </ul>
        </div>
    </div>
    </section>
</div>
{% endblock %}
