<form method="post" action="{{ path('app_client_delete', {'cin': client.cin}) }}" onsubmit="return confirm('Are you sure you want to delete this item?');">
    <input type="hidden" name="_token" value="{{ csrf_token('delete' ~ client.cin) }}">
    <button class="btn">Delete</button>
</form>
